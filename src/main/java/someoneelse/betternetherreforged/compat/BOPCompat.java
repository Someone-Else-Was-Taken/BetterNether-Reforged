package someoneelse.betternetherreforged.compat;


import someoneelse.betternetherreforged.util.ModGet;

public class BOPCompat {
    static {
        if (ModGet.isLoaded("infernalexp")) {
        }

        if (ModGet.isLoaded("cinderscapes")) {
        }
    }
}
